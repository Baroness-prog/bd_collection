<div class="d-flex justify-content-center my-3">
    <h1 class="col-10 bg-green text-light text-center p-2 m-3 rounded-3 fw-bold fs-3">Ajouter une BD à ma Wishlist</h1>
</div>
<div class="justify-content-center col-10 mx-auto">
    {{ form_start(form) }}
    {{ form_widget(form) }}
</div>
<div class="d-flex justify-content-center mt-4 mb-2 p-2">
    <button type="submit" class="btn btn-success text-light rounded-pill fw-bold fs-5">Valider</button>
</div>
{{ form_end(form) }}